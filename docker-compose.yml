version: '3'
services:
  # collector:
  #   build: ./collector
  #   env_file: ./dev.env
  #   depends_on:
  #     - mongo
  #   links:
  #     - mongo
  #   ports:
  #     - 8080:8080
  # specialist_app:
  #   build: ./specialist-app
  #   env_file: ./dev.env
  #   ports:
  #     - 3000:3000
  # tasks:
  #   build: ./tasks
  #   links:
  #     - mongo
  #   env_file: ./dev.env
  specialist_api:
    build: ./specialist-api
    ports:
      - 8081:8081
    links:
      - mongo
  mongo:
    image: mongo:latest
    ports:
      - 27017:27017
    volumes:
      - ./data:/data/db